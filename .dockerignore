# Git
.git
.gitignore

# Docker files (not needed inside the build context)
Dockerfile
compose.yml
.dockerignore

# Runtime artifacts
*.db
xmpp-releasetracker

# Config with real credentials — never bake into the image
config.yml

# Docs
README.md
